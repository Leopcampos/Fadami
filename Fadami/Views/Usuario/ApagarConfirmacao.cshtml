@model Usuario

@{
    ViewData["Title"] = "Confirmação apagar o usuário";
}

<div class="text-center">
    <h3>@ViewData["Title"] (@Model.Nome)?</h3>
</div>

<div class="d-grid gap-2">
<a role="button" class="btn btn-primary" asp-controller="Usuario" asp-action="Index">Cancelar exclusão</a>
<a role="button" class="btn btn-danger" asp-controller="Usuario" asp-action="Apagar" asp-route-id="@Model.Codigo">Confirmar exclusão</a>
</div>